<!DOCTYPE html>
<html>
  <head>
    <title>Tripplan</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body data-controller="menu">
    <%= render "shared/flash" %>
    <% show_app_shell = !((controller_name == "pages" && action_name == "home") || devise_controller?) %>
    <% if show_app_shell %>
      <%= render "shared/app_header" %>
      <main class="tp-page">
        <%= render "shared/container" do %>
          <%= yield %>
        <% end %>
      </main>
      <%= render "shared/hamburger_menu" %>
    <% else %>
      <%= yield %>
      <% if controller_name == "pages" && action_name == "home" && user_signed_in? %>
        <%= render "shared/hamburger_menu" %>
      <% end %>
    <% end %>
    <% if demo_user? && !devise_controller? %>
      <%= render "shared/demo_reset" %>
    <% end %>
  </body>
</html>
